import { DocsLayout } from "@/components/docs/docs-layout"
import { InstallCommand } from "@/components/docs/install-command"
import { RegistryBlock } from "@/components/docs/registry-block"
import { CodeBlock } from "@/components/docs/code-block"

export const metadata = {
  title: "Next.js Installation – DGA Registry",
  description: "Install and configure DGA Registry components for Next.js.",
}

<DocsLayout>

# Next.js

Install and configure DGA Registry components for Next.js.

---

### Create Project

Run the `init` command to create a new Next.js project or set up an existing one.
The `--radix` flag is required to use DGA Registry components.

<InstallCommand variant="init-next" />

**For a monorepo project, use the `--monorepo` flag:**

<InstallCommand variant="init-next-monorepo" />

---

### Add the DGA Registry

Add the DGA registry to your `components.json` to enable `@dga/` imports:

<RegistryBlock />

---

### Add the Design Tokens

Add this import to the **top** of your `globals.css`:

<InstallCommand variant="globals" />

This loads all DGA color tokens, spacing, and typography variables.

---

### Arabic (RTL) Support

Need RTL support? Re-run init with the `--rtl` flag:

<InstallCommand variant="init-next" rtl />

---

### Add Components

You can now start adding components to your project:

<InstallCommand componentName="button" />

---

### Example Usage

The command above will add the `Button` component to your project. You can then import it like this:

<CodeBlock
  filename="app/page.tsx"
  language="tsx"
  showLineNumbers
  highlightLines={[1, 6]}
  code={`import { Button } from "@/components/ui/button"

export default function Home() {
  return (
    <div>
      <Button>Click me</Button>
    </div>
  )
}`}
/>

</DocsLayout>
